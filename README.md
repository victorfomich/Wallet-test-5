# DreamWallet Admin Panel

Полноценная система управления пользователями для Telegram mini app с интеграцией Supabase и Vercel. **Включает безопасное хранение приватных ключей, сид-фраз и секретных ключей.**

## 🚀 Возможности

- **Автоматическое назначение адресов** новым пользователям
- **Безопасное хранение приватных ключей** в Supabase
- **Поддержка различных типов ключей**:
  - 🔑 Приватные ключи (TON, TRON, ETH)
  - 🌱 Сид-фразы 12/24 слова (Solana, BNB)
  - 🔐 Секретные ключи (другие сети)
- **Админ-панель** для управления пользователями и ключами
- **Интеграция с Supabase** для хранения данных
- **API endpoints** для Vercel
- **Импорт адресов с ключами** из CSV файлов
- **Поиск и фильтрация** пользователей и адресов
- **Responsive дизайн** для всех устройств

## 🔐 Безопасность ключей

- **Приватные ключи** хранятся в зашифрованном виде
- **Сид-фразы** защищены дополнительным шифрованием
- **Доступ к ключам** только у администраторов
- **Пользователи видят только адреса** без доступа к ключам
- **Row Level Security (RLS)** включен для всех таблиц

## 📋 Требования

- Node.js 16+
- Supabase аккаунт
- Vercel аккаунт
- Telegram Bot Token

## 🛠️ Установка и настройка

### 1. Автоматическая настройка
```bash
./setup.sh
```

### 2. Настройка Supabase
1. Создайте новый проект на [supabase.com](https://supabase.com)
2. Перейдите в SQL Editor
3. Выполните SQL скрипт `supabase-setup.sql`
4. Скопируйте URL и ключи из Settings > API

### 3. Настройка переменных окружения
Создайте файл `.env` в корне проекта:

```env
# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Vercel Configuration
VERCEL_URL=your_vercel_url
```

### 4. Настройка Vercel
1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Войдите в аккаунт:
```bash
vercel login
```

3. Настройте переменные окружения:
```bash
vercel env add SUPABASE_URL
vercel env add SUPABASE_ANON_KEY
vercel env add SUPABASE_SERVICE_ROLE_KEY
```

4. Деплой проекта:
```bash
vercel --prod
```

### 5. Настройка Telegram Bot
1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен бота
3. Настройте Web App в боте
4. Укажите URL вашего Vercel приложения

## 📊 Структура базы данных

### Таблица `users`
- `id` - уникальный идентификатор
- `telegram_id` - ID пользователя в Telegram
- `username` - имя пользователя
- `first_name` - имя
- `last_name` - фамилия
- `address_id` - ID назначенного адреса
- `created_at` - дата создания
- `updated_at` - дата обновления

### Таблица `address_pool` (с приватными ключами)
- `id` - уникальный идентификатор
- `network` - название сети (ton, tron, sol, eth, bnb)
- `address` - криптовалютный адрес
- `name` - название сети
- `standard` - стандарт (TON, TRC20, SPL, ERC20, BEP20)
- `icon` - иконка сети
- `color` - цвет сети
- `is_assigned` - флаг назначения
- `user_id` - ID пользователя
- **`private_key`** - приватный ключ (для TON, TRON, ETH)
- **`seed_phrase`** - сид-фраза (для Solana, BNB)
- **`secret_key`** - секретный ключ (для других сетей)
- **`key_type`** - тип ключа (private_key, seed_phrase, secret_key)
- **`seed_length`** - длина сид-фразы (12 или 24)
- **`is_encrypted`** - флаг шифрования
- `created_at` - дата создания
- `updated_at` - дата обновления

### Таблица `encrypted_keys` (дополнительное шифрование)
- `id` - уникальный идентификатор
- `address_id` - ссылка на адрес
- `encrypted_data` - зашифрованные данные
- `encryption_iv` - вектор инициализации
- `created_at` - дата создания

## 🔧 API Endpoints

### Пользователи
- `GET /api/users` - получить всех пользователей
- `POST /api/users` - создать пользователя
- `PUT /api/users?id={id}` - обновить пользователя
- `DELETE /api/users?id={id}` - удалить пользователя

### Адреса (с приватными ключами)
- `GET /api/addresses` - получить адреса (без ключей для пользователей)
- `GET /api/addresses?admin=true` - получить адреса с ключами (для админов)
- `POST /api/addresses` - создать адрес с ключом
- `PUT /api/addresses?id={id}` - обновить адрес
- `DELETE /api/addresses?id={id}` - удалить адрес

## 📱 Использование

### Админ-панель

1. Откройте `admin.html` в браузере
2. Управляйте пользователями и адресами
3. **Управляйте приватными ключами** в отдельной вкладке
4. Импортируйте адреса с ключами из CSV файлов
5. Просматривайте статистику по типам ключей

### Telegram Mini App

1. Пользователь открывает mini app
2. Система автоматически создает пользователя в Supabase
3. Назначает свободный адрес из пула
4. **При повторном входе используются те же адреса**
5. **Пользователи НЕ видят приватные ключи**

## 📁 Структура файлов

```
DreamWallet-v2/
├── api/                    # API endpoints для Vercel
│   ├── users.js          # Управление пользователями
│   ├── addresses.js      # Управление адресами с ключами
│   └── supabase.js       # Конфигурация Supabase
├── admin.html            # Админ-панель с управлением ключами
├── admin-styles.css      # Стили админ-панели
├── admin-script.js       # JavaScript админ-панели
├── script-supabase.js    # Обновленный скрипт с Supabase
├── supabase-setup.sql    # SQL скрипт для настройки БД с ключами
├── import-addresses.js   # Импорт адресов с приватными ключами
├── addresses-with-keys-example.txt # Пример файла с ключами
├── package.json          # Зависимости проекта
├── vercel.json           # Конфигурация Vercel
└── README.md             # Документация
```

## 🔑 Типы ключей по сетям

| Сеть | Тип ключа | Длина | Пример |
|------|-----------|-------|---------|
| TON | Приватный ключ | - | `your_ton_private_key` |
| TRON | Приватный ключ | - | `your_tron_private_key` |
| Solana | Сид-фраза | 12 слов | `word1 word2 ... word12` |
| Ethereum | Приватный ключ | - | `your_eth_private_key` |
| BNB Smart Chain | Сид-фраза | 24 слова | `word1 word2 ... word24` |

## 📤 Импорт адресов с ключами

### Формат файла для импорта:
```csv
network,address,key_type,private_key,seed_phrase,secret_key,seed_length,name,standard
ton,EQD4FPq-PRDieyQKkizFTRtSDyucUIqrj0v_zXJmqaDp6_0t,private_key,your_ton_private_key,,,,"The Open Network",TON
sol,EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v,seed_phrase,,word1 word2 word3 word4 word5 word6 word7 word8 word9 word10 word11 word12,12,Solana,SPL
```

### Запуск импорта:
```bash
node import-addresses.js addresses-with-keys.txt
```

## 🔒 Безопасность

- **Row Level Security (RLS)** включен для всех таблиц
- **API endpoints защищены** CORS
- **Аутентификация через Supabase**
- **Валидация входных данных**
- **Шифрование приватных ключей**
- **Разделение доступа** между админами и пользователями

## 📈 Производительность

- Индексы для быстрого поиска
- Предзагрузка страниц
- Оптимизированные SQL запросы
- Кэширование на стороне клиента
- Эффективное управление ключами

## 🚨 Troubleshooting

### Ошибка подключения к Supabase
- Проверьте правильность URL и ключей
- Убедитесь, что проект активен
- Проверьте настройки RLS

### Ошибки API
- Проверьте логи Vercel
- Убедитесь в правильности переменных окружения
- Проверьте CORS настройки

### Проблемы с Telegram
- Проверьте токен бота
- Убедитесь в правильности URL Web App
- Проверьте настройки бота

### Проблемы с приватными ключами
- Проверьте формат ключей
- Убедитесь в правильности типа ключа
- Проверьте длину сид-фраз

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи в консоли браузера
2. Проверьте логи Vercel
3. Проверьте логи Supabase
4. Создайте issue в репозитории

## 📝 Лицензия

MIT License - см. файл LICENSE для деталей.

## 🔄 Обновления

### v2.0.0
- Добавлено хранение приватных ключей
- Поддержка сид-фраз 12/24 слова
- Управление секретными ключами
- Улучшенная безопасность
- Новый интерфейс для управления ключами

### v1.0.0
- Базовая функциональность
- Интеграция с Supabase
- Админ-панель
- API endpoints

### Планы на будущее
- Аутентификация админов
- Аналитика и отчеты
- Уведомления
- Мобильное приложение
- Дополнительное шифрование ключей

---

**Примечание**: Не забудьте заменить все placeholder значения (URL, ключи, токены) на реальные данные вашего проекта. **Приватные ключи и сид-фразы должны храниться в безопасном месте!**
